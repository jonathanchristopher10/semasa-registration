Stand-in QR block pattern for mocks and prototypes — deterministic from `value`, with real finder squares, so screenshots look right. Not scannable.

```jsx
<QrCode value="SP26-7K2QD9" size={200} />
```

In production the app renders `qrcode.react` at the same size and quiet zone; keep the white padding so the code reads on cream.
