The padded column inside PhoneFrame — 20px gutters, 24px block rhythm, `position: relative` so Stickers can be placed against it.

```jsx
<Screen>
  <ScreenHeader title="Visit Details" />
  <Card>…</Card>
</Screen>
```

Use `padded={false}` only for full-bleed hero art.
