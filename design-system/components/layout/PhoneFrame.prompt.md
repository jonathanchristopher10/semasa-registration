Desktop wrapper for the mobile-first app: 430×900, 3px ink outline, 44px corners, soft lift shadow, cream gradient inside.

```jsx
<PhoneFrame>
  <Screen />
</PhoneFrame>
```

The frame is presentation only — screens inside must still lay out fluidly at 390–430px. Never nest a frame inside a frame.
