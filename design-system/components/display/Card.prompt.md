Outlined rounded panel — the base container for every block of content.

```jsx
<Card tone="white" padding={20}>…</Card>
<Card tone="cream" radius="var(--radius-card-lg)" softShadow>…</Card>
```

Ink outline on, shadow off is the default look; the outline carries the sticker feel, so don't trade it for a shadow.
